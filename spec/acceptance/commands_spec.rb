require 'spec_helper'

describe Ignatius::Generators::AppGenerator do
  DEFAULT_FILES = %w(
    robot.txt
    humans.txt
    config.rb
    config.ru
    _config.yml
  )

  let(:default_files) { DEFAULT_FILES }

  it 'should create structure base for blog' do
  end
end
